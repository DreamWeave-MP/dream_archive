# BSA test fixtures

These fixtures are tracked so BSA tests are reproducible in a clean checkout and
in CI. They were copied from the `bsa-rs` fixture corpus and placed by format
generation under `tests/fixtures/bsa/`.

The upstream fixture corpus is licensed under the BSD Zero Clause License
(`0BSD`), which permits reuse, modification, and redistribution. This repository
is licensed as `GPL-3.0-only`.
